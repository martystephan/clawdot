/**
 * Disclosure primitive (Root/Trigger/Panel), re-exported from Base UI.
 * Unstyled by design — callers attach their own classNames; the Panel
 * unmounts its children while closed.
 */
export { Collapsible } from "@base-ui/react/collapsible";
